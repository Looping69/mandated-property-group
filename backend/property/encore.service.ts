import { Service } from "encore.dev/service";

// Define the property service
// This manages all property-related logic, including listings, agents, and inquiries
export default new Service("property");
